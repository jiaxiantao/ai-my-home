---
description: 每次对话有代码改动时，在结束前提交到本地 git 仓库
alwaysApply: true
---

# Git 提交约定

当本次对话中对仓库产生了代码或配置改动时，在回复用户前完成一次本地 git 提交：

1. 并行执行 `git status`、`git diff`、`git log -1 --oneline` 了解变更与提交风格。
2. 不要提交 `.env` 或含密钥的文件；`.env.example` 可以提交。
3. 用 HEREDOC 写 1–2 句英文或中文 commit message，说明「为什么」。
4. 执行 `git add`（仅相关文件）→ `git commit` → `git status` 确认干净。
5. 除非用户明确要求，不要 `git push`。
6. 若无任何可提交改动，不要创建空提交。

在最终回复中简短告知：commit hash、分支名、是否已 push。
